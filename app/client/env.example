# =============================================================================
# Agent HQ Client - Environment Variables
# =============================================================================
# Rename this file to .env and fill in your actual values
# For React (Create React App), all environment variables must start with REACT_APP_
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Backend API URL - use relative path so requests route through the webpack
# dev server proxy (setupProxy.js), which handles port mapping for worktrees
REACT_APP_API_URL=/api

# Optional: Add additional React app config below
# REACT_APP_DEBUG=true
# REACT_APP_VERSION=1.0.0
